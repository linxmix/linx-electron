// const { combineRules } = require('fela')


// const drop = ({ isOverCurrent, canDrop }) => {
//   if (isOverCurrent && canDrop) return {
//     borderBottom: '2px solid green'
//   }
// }

// const row = (props) => ({

// })

module.exports = {
  // container: combineRules(row, drop)
  container: {}
}

/*

    const dropClassName = (isOverCurrent && canDrop) ? 'primary-track-row-drag-over' : ''
    const draggableClassName = canDrag ? 'primary-track-row-draggable' : ''
    const draggingClassName = isDragging ? 'primary-track-row-dragging' : ''

.primary-track-row .delete-button {
  cursor: pointer;
}

.primary-track-row .edit-button {
  cursor: pointer;
}

.primary-track-row .edit-buttons {
  visibility: hidden;
}

.primary-track-row:hover .edit-buttons {
  visibility: visible;
}

.primary-track-row-drag-over {
  border-bottom: 2px solid green;
}

.primary-track-row-dragging {
  opacity: 0.5;
}

.primary-track-row-draggable:hover {
  cursor: pointer;
}
*/
